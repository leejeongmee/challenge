function focusevent(id) {
  let focusev = document.getElementById(id);
  // focusev.style.borderColor = 'green';
}

function blurevent(id) {
  let blurev = document.getElementById(id);
  // focusev.style.borderColor = '#cccccc';
}

focusevent(id);
blurEffect(id);
